/**
 * * important info
 * ! deprecated
 * TODO: do this
 * ? why?
 */

/**
 * ? how to check list of global packages
 * npm list -g --depth 0
 */

/**
 * ? why Typescript?
 * TS Handbook "The goal of TypeScript is to be a static typechecker for JavaScript
 * programs - in other words, a tool that runs before your code runs (static)
 * and ensures that the types of the program are correct (typechecked)."
 *
 * * how to install typescript
 * npm init -> to init a project (package.json)
 * npm install typescript -D -> to install typescript as dev-dependency
 * npm install ts-node -D -> wrapper around node working for typescript
 * npx tsc --init -> to initialise a typescript config file
 *
 * * to use ts-node
 * npx ts-node <filename>.ts (or .js)
 *
 * * to run typescript compiler on a file
 * npx tsc <filename>.ts
 */

/**
 *  * ctrl+K + ctrl+I will help define correct types syntax
 */

/**
 * TODO: lesson #1
 * * basics types
 * ? basics.ts
 * TODO: lesson #2
 * * create/use/import/export functions
 * ? functions.ts
 * ? functions-test.ts
 * ? js-functions-test.js
 * ! types are enforced at compiletime not runtime
 * ? what does compile-time means?
 * ? what does run-time means?
 * TODO: lesson #3
 * * functions calling functions / returning functions / closure
 * ? funcs-and-funcs.ts
 * TODO: lesson #4
 * * functions overloading
 * ? parseCoordinate.ts
 * TODO: challenge-1
 * * functions overloading
 * ? challenge-1.ts
 * TODO: lesson #5
 * * optionals
 * ? optionals.ts
 * TODO: lesson #6
 * * tuples
 * ? tuples.ts
 * TODO: lesson #7
 * * generics
 * ? generics.ts
 * TODO: challenge-2
 * * generics
 * ? challenge-2.ts
 * TODO: lesson #8
 * * generics (keyof)
 * ? generics-with-keyof.ts
 * TODO: lesson #9
 * * utility types (Record / Pick / Omit / Required)
 * ? utility-types-1.ts
 * TODO: lesson #10
 * * utility types (readonly / immutability)
 * ? readonly-1.ts
 * TODO: lesson #11
 * * enum and literal types
 * ? enums.ts
 * TODO: lesson #12
 * * TS classes
 * ? database.ts
 * TODO: lesson #13
 * * generics integration with classes
 * ? database.ts
 * TODO: lesson #
 * *
 * ? .ts
 * TODO: lesson #
 * *
 * ? .ts
 * TODO: lesson #
 * *
 * ? .ts
 * TODO: lesson #
 * *
 * ? .ts
 * TODO: lesson #
 * *
 * ? .ts
 * TODO: lesson #
 * *
 * ? .ts
 * TODO: lesson #
 * *
 * ? .ts
 * TODO: lesson #
 * *
 * ? .ts
 */
